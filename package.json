{
  "name": "idle-awakening-modular-ui",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "audit:bundle": "node scripts/audit-bundle.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
