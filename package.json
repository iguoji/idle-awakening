{
  "name": "idle-awakening-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "recover": "node scripts/extract-webpack-sources.mjs --main bundle.js --worker bundle.worker.js --out src/recovered"
  },
  "devDependencies": {
    "vite": "^7.1.4"
  }
}
